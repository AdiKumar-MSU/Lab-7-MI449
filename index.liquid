---
layout: layout.liquid
title: Quotes
---

<div class="container mx-auto p-4"
    x-data="{ 
        randomInt: null,
        generateRandomNumber() {
            this.randomInt = window.getRandomInt(1454);
        }
    }"
>   
    <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded transition duration-300" @click="generateRandomNumber()">Get A Random Quote 💬</button>
    <h2 class="font-bold text-white mt-5" x-text="await $fetchjson(`https://dummyjson.com/quotes/${randomInt}`, jsonItem='quote')"></h2>
    
    <template x-if="randomInt">
        <h3 class="text-center mx-auto" x-text="'— ' + await $fetchjson(`https://dummyjson.com/quotes/${randomInt}`, jsonItem='author')"></h3>
    </template>
</div>